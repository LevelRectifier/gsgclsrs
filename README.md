# Junteng Liu — Personal Academic Homepage (`LJT-Homepage`)

Source of the academic homepage of [Junteng Liu](https://github.com/Vicent0205),
built on the [Academic Pages](https://github.com/academicpages/academicpages.github.io)
Jekyll template (a fork of the Minimal Mistakes theme).

> **Repository name / fork status.** The intended location of this content is a
> repository named `LJT-Homepage` in this account, forked from
> `academicpages/academicpages.github.io`. GitHub currently rejects the fork,
> the repository creation and the repository rename request for this account
> with `403 — At least one email address must be verified to do that.`
> (file/commit writes still work), so the site source is committed here in the
> meantime. `_config.yml` is already written for the `LJT-Homepage` name:
>
> ```yaml
> baseurl    : "/LJT-Homepage"
> repository : "LevelRectifier/LJT-Homepage"
> ```
>
> To finish: verify the account e-mail address, fork
> `academicpages/academicpages.github.io` as `LJT-Homepage` (or rename this
> repository to `LJT-Homepage`), copy the files below into it, and remove the
> template's placeholder entries in `_posts/`, `_talks/`, `_teaching/`,
> `_portfolio/` and `_publications/`.

## Where the personal information lives

| Path | Content |
| --- | --- |
| `_config.yml` | Site title, description, repository, and the author sidebar: name, bio ("First-year PhD candidate at HKUST NLP Group"), e-mail `jliugi@connect.ust.hk`, GitHub `Vicent0205`, Google Scholar profile, X (Twitter) `@junteng88716710` |
| `_pages/about.md` | Home / about page: biography, research interests, education, research experience, full publication list, honors and awards, contact information |
| `_pages/publications.html` | Publications sub-page, generated from the `_publications/` collection |
| `_publications/*.md` | One file per paper: SynLogic (arXiv 2025), On the Perception Bottleneck of VLMs for Chart Understanding (arXiv 2025), On the Universal Truthfulness Hyperplane Inside LLMs (EMNLP 2024), In-Context Sharpness as Alerts (ICML 2024), C-Eval (NeurIPS 2023), Composing Parameter-Efficient Modules with Arithmetic Operations (NeurIPS 2023) |
| `_data/navigation.yml` | Header navigation — only the Publications page is linked; no additional pages were added |

All biographic details (academic background, research experience, publications,
skills/interests and contact information) come from the personal record supplied
for this page; nothing else was invented or added.
